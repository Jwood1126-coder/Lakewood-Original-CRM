# Web framework
Flask==3.0.3
Werkzeug==3.0.4

# Database & migrations
SQLAlchemy==2.0.36
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
alembic==1.13.3

# Auth
Flask-Login==0.6.3
Flask-WTF==1.2.2
WTForms==3.2.1
argon2-cffi==23.1.0

# Forms / validation helpers
email-validator==2.2.0

# Photo handling
Pillow==11.0.0

# Scheduling
APScheduler==3.10.4

# Anthropic SDK (assistant). Pinned >=0.45 because earlier versions
# call httpx.Client(proxies=...) which was removed in httpx 0.28+.
anthropic>=0.45.0,<1.0.0

# Backups (Backblaze B2 via S3-compatible API)
boto3==1.35.49

# PDF generation: deferred to Phase 3.
# xhtml2pdf was pulling in pycairo (needs system Cairo). When Phase 3 lands,
# we'll evaluate fpdf2 (pure Python, no native deps) as the simpler choice.

# Rate limiting (login throttle, future API endpoints)
Flask-Limiter==3.8.0

# OAuth token encryption at rest (Jobber API integration)
cryptography>=43.0.0

# Production server
gunicorn==23.0.0

# Config / env
python-dotenv==1.0.1

# Testing
pytest==8.3.3
pytest-flask==1.3.0
